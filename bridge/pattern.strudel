/* 
   ====================================================
   @title 2026-07-08 Learning
   @by Nick
   ==================================================== 
*/

/*
samples('http://localhost:5432/')

// === GLOBAL CONTROLS (put these at the very top) ===
const globalGain   = slider(0.12, 0, 1, 0.01);   // main volume slider
const globalRoom   = slider(0.271, 0, 1);           // global reverb amount
const globalDelay  = slider(0.125, 0, 1);           // global delay wet
const globalCutoff = slider(4377.2, 200, 12000);    // global lowpass cutoff (example)

  
$percussion: s("[nx:1 cp hh cp][ bd cp cp cp]").postgain(slider(0.15, 0, 1))._scope()// === GLOBAL APPLICATION (put this at the bottom) ===
all(x => x
  .postgain(globalGain)
  .room(globalRoom)
  .delay(globalDelay)
  .lpf(globalCutoff)
)
*/


/* 
   ====================================================
   To Do & Learn 
   With example repository. 
   ==================================================== 

Environmental.    
      Set CPM. 

Samples.    
      Utilize local samples. 
      Play one long local sample over several measures. 
      Cut up a voice sample. Make it sound cool and techno. 
      Change the pitch and effects on a sample. 

Timing. 
      Understand the different brackets "" <> [] And how to use them effectively. 
      Establish long running non-repeating measures for both notes and percussions. 

Effects. 
      Add cool sounding techno and flange effects. 
   
*/